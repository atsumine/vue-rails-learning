<p id="notice"><%= notice %></p>

<h1>Todos</h1>


<div id="todo_index">
  <form v-on:submit.prevent="addTodo($event)" action="/todos">
    <input type="text" id="new_todo_area">
  </form>
  <table>
    <thead>
    <tr>
      <th>Name</th>
      <th>Deadline</th>
      <th>Done</th>
      <th></th>
    </tr>
    </thead>

    <tbody v-for="(todo, index) in todos">
      <tr>
        <td>{{ todo.name }}</td>
        <td>{{ todo.deadline }}</td>
        <td><{{ todo.done }}</td>
        <td><input type="button" v-on:click="deleteTodo(index)" value="delete"></td>
      </tr>
    </tbody>
  </table>
  <br>

  <%= link_to 'New Todo', new_todo_path %>
</div>
